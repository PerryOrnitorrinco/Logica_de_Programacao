/*    
    strcpy (<destino>, <origem>); // Na tag 'destino' e informado o nome da string que vai receber a copia; e na tag 'origem' o nome da string que sera copiada. Podendo ser realmente uma string ou um texto entre aspas.

    strcat (<destino>, <origem>); // Quando se quer 'colar' uma string na outra.");

    strlen (<string); // Mostra o comprimento da string

    strcmp (<string1>, <string2>); // Compara se as duas strings são estritamente igual. Caso sim, o strcmp retorna o valor '0', então, basta compara essa função com '0', que você consegue um saber se as strings são iguais.

*/